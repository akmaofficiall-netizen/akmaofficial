"use client";
import React,{useState} from "react"; import {useRouter} from "next/navigation"; import {LogIn} from "lucide-react";
export default function EmployeeLoginPage(){const r=useRouter();const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const x=await fetch("/api/auth/employee-login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})}).then(v=>v.json());if(!x.success)return setError(x.error||"ورود ناموفق بود");r.push("/employee-dashboard");}finally{setLoading(false)}};return <main className="min-h-screen bg-slate-950 text-white flex items-center justify-center p-6 dir-rtl"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900/80 p-7 space-y-5 shadow-2xl"><div><h1 className="text-2xl font-black">ورود همکار</h1><p className="text-xs text-slate-400 mt-1">ورود به داشبورد اختصاصی همکار</p></div><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="نام کاربری / موبایل" className="w-full rounded-xl bg-slate-950 border border-slate-800 p-3 text-sm"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="رمز عبور" className="w-full rounded-xl bg-slate-950 border border-slate-800 p-3 text-sm"/>{error&&<p className="text-xs text-rose-300">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-purple-600 py-3 font-bold flex items-center justify-center gap-2"><LogIn className="h-4 w-4"/>{loading?"در حال ورود...":"ورود"}</button></form></main>}
